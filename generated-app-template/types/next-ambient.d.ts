/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Committed ambient type references for the Next.js app. Unlike the generated
// `next-env.d.ts` (gitignored), this file does not reference any build-generated
// file (e.g. .next/types/routes.d.ts), so a fresh-checkout `tsc --noEmit`
// succeeds before the first `next build`. `next-env.d.ts` is intentionally kept
// out of the tsconfig `include` so tsc never follows its generated references.
